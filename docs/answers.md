1. Points across a span — even split, per-segment allocation, or first-sprint-only?

- add the points to the first sprint its in, if it stretched add the points in a seperate section called creep or in ()

2. Capacity — are we building the roster + velocity model, or do I cut PTO/holidays from v1?

- If possible, we should count number of work days (m,t,w,th,f) in the sprint and show its available bandwidth,( it should be multiplied by number of assignees. we should have a way to list memmbers of this epic/work for easy assigning), when adding tickets, the points should shown, and there should be a balance that subtracts the points allocated from the bandiwith, it should be green if above 5, yellow between 5 and 0 and red if negative

3. Blocked By — ticket reference or free text?

- should be a reference but also disallow it from being put on the board if its blocking ticket has not been placed yet, it should also only allow the blocked ticket to be placed in the same sprint or future spritns as the ticket that blocks it

4. Sprint dates — cadence-generated from a start date, or manual per sprint?
   - It should be per sprint, but we can automate setup but asking for sprint count, and length of sprint in days, and then repopulate the dates like this but allow for editting
5. Jira later — will you accept a small proxy backend, or does this stay a static site forever (which means Jira Cloud is off the table)?

- lets discuss this.
